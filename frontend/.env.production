# Build-time backend for Vercel deployments. The URL is public (the VPS serves
# the API) and is intentionally tracked so any origin works without dashboard env.
VITE_API_URL=https://aios-vps.tail59eb2d.ts.net